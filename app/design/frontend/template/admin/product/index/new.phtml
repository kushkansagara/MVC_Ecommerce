<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* Base Container */
        .product-form-container {
            padding: 35px;
            background: #ffffff;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
            margin: 20px auto;
            width: 90%;
            max-width: 800px;
            border-radius: 15px;
            transition: all 0.3s ease;
        }

        .product-form-container:hover {
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
        }

        /* Headings */
        .product-form-heading {
            text-align: center;
            color: #3a7bd5;
            margin-bottom: 25px;
            font-size: 2.2em;
            font-weight: 600;
            position: relative;
            padding-bottom: 10px;
        }

        .product-form-heading::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: linear-gradient(to right, #3a7bd5, #00d2ff);
            border-radius: 2px;
        }

        .product-form-section-heading {
            text-align: center;
            color: #2d5ca0;
            margin: 35px 0 20px;
            font-size: 1.8em;
            position: relative;
            padding-bottom: 10px;
        }

        .product-form-section-heading::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 2px;
            background: linear-gradient(to right, #3a7bd5, #00d2ff);
            border-radius: 2px;
        }

        /* Form Elements */
        .product-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
            margin-top: 30px;
        }

        .product-form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin-bottom: 15px;
            animation: productFormFadeIn 0.5s ease forwards;
        }

        .product-form-label {
            font-weight: 600;
            color: #495057;
            font-size: 1.05em;
            transition: color 0.3s ease;
            display: block;
            margin-bottom: 5px;
        }

        .product-form-input,
        .product-form-textarea,
        .product-form-select {
            padding: 14px;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            font-size: 1em;
            transition: all 0.3s ease;
            width: 100%;
            background-color: #f8f9fa;
        }

        .product-form-input:focus,
        .product-form-textarea:focus,
        .product-form-select:focus {
            outline: none;
            border-color: #3a7bd5;
            box-shadow: 0 0 0 3px rgba(58, 123, 213, 0.2);
            background-color: #fff;
        }

        .product-form-input:focus+.product-form-label,
        .product-form-textarea:focus+.product-form-label,
        .product-form-select:focus+.product-form-label {
            color: #3a7bd5;
        }

        .product-form-textarea {
            min-height: 120px;
            resize: vertical;
        }

        /* Submit Button */
        .product-form-submit {
            padding: 14px;
            background: linear-gradient(to right, #3a7bd5, #00d2ff);
            color: #fff;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1.05em;
            font-weight: 600;
            transition: all 0.3s ease;
            margin: 20px auto 0;
            letter-spacing: 0.5px;
            width: 100%;
            max-width: 200px;
            display: block;
        }

        .product-form-submit:hover {
            background: linear-gradient(to right, #3a6fc7, #00bed9);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(58, 123, 213, 0.4);
        }

        .product-form-submit:active {
            transform: translateY(0);
        }

        /* Dropdown Styling */
        .product-form-select {
            appearance: none;
            background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23495057' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
            background-repeat: no-repeat;
            background-position: right 15px center;
            background-size: 15px;
            padding-right: 40px;
        }

        /* File Input Styling */
        .product-form-file {
            padding: 10px;
            background-color: #f8f9fa;
            border: 2px dashed #e2e8f0;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .product-form-file:hover {
            border-color: #3a7bd5;
            background-color: rgba(58, 123, 213, 0.05);
        }

        /* Attribute Container */
        .product-attribute-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-bottom: 25px;
        }

        .product-attribute-item {
            animation: productFormFadeIn 0.5s ease forwards;
            padding: 15px;
            border-radius: 8px;
            background-color: #f8f9fa;
            border: 1px solid #e2e8f0;
        }

        /* Section Dividers */
        .product-form-section {
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid #e2e8f0;
        }

        /* Validation Styling */
        .product-form-error {
            color: #e74c3c;
            font-size: 0.9em;
            margin-top: 5px;
        }

        .product-form-input-error,
        .product-form-select-error,
        .product-form-textarea-error {
            border-color: #e74c3c;
        }

        .product-form-success {
            background-color: #d4edda;
            color: #155724;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Media Gallery Section */
        .product-media-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }

        .product-media-preview {
            border: 1px solid #e2e8f0;
            border-radius: 5px;
            overflow: hidden;
            position: relative;
            padding-top: 100%;
        }

        .product-media-preview img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Animation */
        @keyframes productFormFadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Delay animations for sequenced appearance */
        .product-form-group:nth-child(1) {
            animation-delay: 0.1s;
        }

        .product-form-group:nth-child(2) {
            animation-delay: 0.2s;
        }

        .product-form-group:nth-child(3) {
            animation-delay: 0.3s;
        }

        .product-form-group:nth-child(4) {
            animation-delay: 0.4s;
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .product-form-container {
                padding: 25px;
                width: 95%;
            }

            .product-form-heading {
                font-size: 1.8em;
            }

            .product-form-section-heading {
                font-size: 1.5em;
            }

            .product-form-input,
            .product-form-textarea,
            .product-form-select,
            .product-form-submit {
                padding: 12px;
            }

            .product-form-submit {
                width: 100%;
            }

            .product-media-gallery {
                grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            }
        }

        /* Accessibility Focus */
        .product-form-input:focus-visible,
        .product-form-textarea:focus-visible,
        .product-form-select:focus-visible,
        .product-form-submit:focus-visible,
        .product-form-file:focus-visible {
            outline: 3px solid rgba(58, 123, 213, 0.5);
            outline-offset: 2px;
        }
    </style>
</head>

<body>
    <div class="product-form-container">
        <?php
        $catalog_product = $this->getProduct();
        $attributes = $this->getAttributeCol();
        $attributee = $this->getAttribute();
        $categories = $this->getCategories();
        // $product = $this->getProduct();
        
        $productAttributes = [];
        foreach ($attributee as $attribute) {
            $productAttributes[$attribute->getAttributeId()] = htmlspecialchars($attribute->getValue());
        }
        ?>
        <h1 class="product-form-heading">Add Product</h1>

        <?php echo $this->getHtml('formStart', [
            'method' => 'POST',
            'action' => $this->getUrl('*/*/save'),
            'enctype' => 'multipart/form-data',
            'class' => 'product-form'
        ]); ?>

        <div class="product-form-section">
            <?php echo $this->getHtml('hidden', [
                'id' => 'product_id',
                'name' => "catalog_product[product_id]",
                'value' => isset($catalog_product) ? htmlspecialchars($catalog_product->getProductId()) : ""
            ]); ?>

            <div class="product-form-group">
                <?php echo $this->getHtml('label', [
                    'for' => 'name',
                    'class' => 'product-form-label'
                ]); ?>
                <?php echo $this->getHtml('text', [
                    'id' => 'name',
                    'name' => "catalog_product[name]",
                    'class' => 'product-form-input',
                    'value' => isset($catalog_product) ? htmlspecialchars($catalog_product->getName()) : ""
                ]); ?>
            </div>


            <div class="product-form-group">
                <?php echo $this->getHtml('label', ['for' => 'description']); ?>
                <?php echo $this->getHtml('textarea', [
                    'id' => 'description',
                    'class' => 'product-form-input',
                    'name' => "catalog_product[description]",
                    'value' => isset($catalog_product) ? htmlspecialchars($catalog_product->getDescription()) : ""
                ]); ?>
            </div>


            <div class="product-form-group">
                <?php echo $this->getHtml('label', ['for' => 'sku']); ?>
                <?php echo $this->getHtml('text', [
                    'id' => 'sku',
                    'class' => 'product-form-input',
                    'name' => "catalog_product[sku]",
                    'value' => isset($catalog_product) ? htmlspecialchars($catalog_product->getSku()) : ""
                ]); ?>
            </div>


            <div class="product-form-group">
                <?php echo $this->getHtml('label', ['for' => 'price']); ?>
                <?php echo $this->getHtml('text', [
                    'id' => 'price',
                    'class' => 'product-form-input',
                    'name' => "catalog_product[price]",
                    'value' => isset($catalog_product) ? htmlspecialchars($catalog_product->getPrice()) : ""
                ]); ?>
            </div>

            <div class="product-form-group">
                <?php echo $this->getHtml('label', ['for' => 'stock_quantity']); ?>
                <?php echo $this->getHtml('text', [
                    'id' => 'stock_quantity',
                    'class' => 'product-form-input',
                    'name' => "catalog_product[stock_quantity]",
                    'value' => isset($catalog_product) ? htmlspecialchars($catalog_product->getStockQuantity()) : "0"
                ]); ?>
            </div>

            <div class="product-form-group">
                <?php echo $this->getHtml('label', ['for' => 'category_id']); ?>
                <?php echo $this->getHtml('dropDown', [
                    'name' => "catalog_product[category_id]",
                    'class' => 'product-form-input',
                    'options' => $categories,
                    'checked' => isset($catalog_product) ? $catalog_product->getCategoryId() : ""
                ]); ?>
            </div>
        </div>


        <h2 class="product-form-section-heading">Add Attributes</h2>
        <div class="product-form-section">
            <div class="product-attribute-container">
                <?php
                foreach ($attributes as $data): ?>
                    <div class="mb-3">
                        <?php

                        echo $this->getHtml('label', [
                            'for' => $data->getName(),
                            'value' => $data->getName(),
                            'class' => 'form-label fw-bold'
                        ]);
                        $name = "get{$data->getName()}";
                        $value = isset($catalog_product) ? $catalog_product->$name() : '';
                        echo $this->getHtml($data->getType(), [
                            'id' => "id{$data->getAttributeId()}",
                            'name' => "catalog_product[{$data->getName()}]",
                            'value' => $value,
                            'class' => 'form-control'
                        ]);
                        echo $this->getHtml('hidden', [
                            'name' =>
                                "catalog_attribute[{$data->getAttributeId()}][attribute_id]",
                            'value' =>
                                $data->getAttributeId()
                        ]);
                        ?>
                    </div>

                <?php endforeach; ?>
            </div>
        </div>
        <?php if ($catalog_product):
            ?>
            <div class="mb-4" id="currentFileList">
                <label class="form-label fw-bold">Current Images</label>
                <div class="row g-2 flex-column">
                    <?php foreach ($catalog_product->getFilepath() as $filepath): ?>
                        <div class="col-auto">
                            <div class="border p-1 rounded text-danger">
                                <?php
                                $baseUrl = Mage::getBaseUrl() ?>
                                <?php echo $this->getHtml('image', [
                                    'src' => "{$baseUrl}{$filepath}",
                                    'alt' => "product image",
                                    'width' => '100px',
                                    'height' => '100px',
                                    'class' => 'img-thumbnail'
                                ]); ?>
                                <?php echo $this->getHtml('label', ["value" => "<i class='bi bi-x-square-fill'></i>", "for" => $filepath]) ?>
                                <?php echo $this->getHtml('checkbox', ["value" => $filepath, "id" => $filepath, "name" => "deletedimg[]"]) ?>
                                <?php echo $this->getHtml('hidden', ["value" => $filepath]) ?>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>
        <?php endif ?>
        <h2 class="product-form-section-heading">Upload Media</h2>
        <div class="product-form-section">
            <div class="product-form-group">
                <label for="media" class="product-form-label">Upload
                    Images/Videos/Documents</label>
                <input type="file" name="catalog_product[file_path][]" id="img" multiple
                    accept="image/*, video/*, .pdf, .doc, .docx" class="product-form-file">
            </div>
            <div id="fileList"></div>
        </div>

        <button type="submit" class="product-form-submit">Submit</button>
        <?php echo $this->getHtml('formend', []); ?>

        <a href="<?php echo $this->getUrl('*/*/list') ?>">List of product</a>

        <script>
            function addAttributeField() {
                const select = document.getElementById('attribute-select');
                const container = document.getElementById('attribute-container');
            cons t selectedOption = select.options[select.selectedIndex];

                if (selectedOption.value) {
                    const attributeId = selectedOption.value;
                    const attributeName = selectedOption.text;
                    const attributeType = selectedOption.getAttribute('data-type');

                    const fieldWrapper = document.createElement('div');
                    fieldWrapper.classList.add('form-group');
                    fieldWrapper.innerHTML = `
            <label>${attributeName}</label>
            <input type="hidden" name="catalog_product_attribute[${attributeId}][attribute_id]" value="${attributeId}">
            <inp ut type="${getInputType(attributeType)}" name="catalog_product_attribute[${attributeId}][value]">
                `;

                    container.appendChild(fieldWrapper);
                    select.selectedIndex = 0;
                }
            }

            function getInputType(attributeType) {
            swit ch(attributeType) {
                case 'number':
                    return 'number';
                case 'boolean':
                    return 'checkbox';
                case 'date':
                    return 'date';
                default:
                    return 'text';
                }
            }
            document.getElementById("img").addEventListener("change", function (event) {
                let fileListDiv = document.getElementById("fileList");
                fileListDiv.innerHTML = "";

                let files = event.target.files;
                for (let i = 0; i < files.length; i++) {
                    let label = document.createElement("label");
                    let
                        radio = document.createElement("input");
                    radio.type = "radio";
                    radio.name = "main_image";
                    radio.value = files[i].name;
                    label.appendChild(radio);
                    label.appendChild(document.createTextNode(" " + files[i].name));
                    fileListDiv.appendChild(label);
                    fileListDiv.appendChild(document.createElement(" br"));
                }
            });
        </script>