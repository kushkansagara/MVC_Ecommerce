<?php $allMessage = $this->getMessage() ?>
<?php if (!is_array($allMessage)) {
    $allMessage = [];
} ?>
<?php foreach ($allMessage as $key => $message): ?>
    <div class="message-box message-box-<?php echo $key ?>">
        <?php $this->{'get' . ucfirst($key)}(); ?>
    </div>
<?php endforeach; ?>
<style>
    .message-box {
        padding: 15px;
        margin-bottom: 15px;
        border-radius: 5px;
        font-family: Arial, sans-serif;
        display: flex;
        align-items: center;
        gap: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        max-width: 100%;
        width: 100%;
        position: relative;
        animation: slideIn 0.5s ease-out, fadeOut 0.5s ease-in forwards;
        animation-delay: 0s, 5s;
    }

    @keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(-50px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes fadeOut {
        from {
            opacity: 1;
            transform: translateY(0);
        }

        to {
            opacity: 0;
            transform: translateY(-50px);
            display: none;
        }
    }


    /* Success Message Box */
    .message-box-success {
        background-color: #dff0d8;
        border: 1px solid #d6e9c6;
        color: #3c763d;
    }

    .message-box-success::before {
        content: '✓';
        font-weight: bold;
        font-size: 20px;
    }

    /* Error Message Box */
    .message-box-error {
        background-color: #f2dede;
        border: 1px solid #ebccd1;
        color: #a94442;
    }

    .message-box-error::before {
        content: '✗';
        font-weight: bold;
        font-size: 20px;
    }

    /* Warning Message Box */
    .message-box-warning {
        background-color: #fcf8e3;
        border: 1px solid #faebcc;
        color: #8a6d3b;
    }

    .message-box-warning::before {
        content: '!';
        font-weight: bold;
        font-size: 20px;
        border: 2px solid #8a6d3b;
        border-radius: 50%;
        width: 20px;
        height: 20px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }
</style>